  <div class="row">
  	<div class="col-md-12">
      <%= form_for(@enrollment, html: {class: "form-horizontal clearfix", role: "form"}) do |f| %>
      <% if @enrollment.errors.any? %>
      <div class="alert alert-danger">
        <a class="close" data-dismiss="alert">&#215;</a>
        <ul>
          <% @enrollment.errors.full_messages.each do |msg| %>
          <%= content_tag :li, msg, :id => "error_#{msg}" if msg.is_a?(String) %>
          <% end %>
        </ul>
      </div>
      <% end %>
       <fieldset>
        <legend>Nova Matrícula</legend>

        <%= render 'form', :f => f %> 

      </fieldset>
      <% end %>
    </div>
  </div>

